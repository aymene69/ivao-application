DATABASE_URL="file:./dev.db"

# The backend listens here. The frontend dev server (port 3000) proxies
# API calls to this port, so this is never opened directly in the browser.
PORT=3333

# Origin allowed by CORS to call this API (the frontend dev server)
FRONTEND_ORIGIN="http://localhost:3000"
